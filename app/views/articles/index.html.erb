<h1>Listing articles</h1>

<%= link_to 'New article', new_article_path %>
<table>
  <th>Title</th>
  <th>Text</th>
  
  <% @articles.each do |article| %>
    <tr>
      <td><%= article.title %></td>
      <td><%= article.text %></td>
    </tr>
  <% end %>
</table>